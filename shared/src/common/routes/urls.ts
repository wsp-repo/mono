/* eslint-disable @typescript-eslint/naming-convention */

const uiPrefix = 'ui';

export const UiUrls = {
  Service1: `${uiPrefix}/service1`,
} as const;

const s2sPrefix = 's2s';

export const S2sUrls = {
  Service1: `${s2sPrefix}/service1`,
} as const;
